# Dataset visualization

Run the visualization to create two images in `viz_outputs/`:

```bash
python visualize_patches.py
```

The script scans `exported_patches/exported_patches` and only includes files whose filename starts with `OED_`.
